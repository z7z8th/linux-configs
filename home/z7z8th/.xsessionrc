# touchpad
# ===
# synclient TapButton1=1
# syndaemon -k -i 1 -d -t -R&

# XSESS_DBG_LOG=$HOME/tmp/gkd.log

xhost +local:

flock -xn /run/lock/app-$(whoami).lock  python path/to/app.py >/dev/null 2>&1 &

